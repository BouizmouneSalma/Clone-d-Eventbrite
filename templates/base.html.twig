<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}EventPro{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <header class="bg-white shadow">
        <nav class="container mx-auto px-6 py-3">
            <div class="flex justify-between items-center">
                <div class="text-xl font-bold text-orange-500">EventPro</div>
                <div class="flex space-x-4">
                    <a href=" class="text-gray-800 hover:text-orange-500">Événements</a>
                    <a href="" class="text-gray-800 hover:text-orange-500">Créer un événement</a>
                    {% if app.user %}
                        <a href="" class="text-gray-800 hover:text-orange-500">Déconnexion</a>
                    {% else %}
                        <a href="" class="text-gray-800 hover:text-orange-500">Connexion</a>
                    {% endif %}
                </div>
            </div>
        </nav>
    </header>

    <main class="container mx-auto px-6 py-8">
        {% block body %}{% endblock %}
    </main>

    <footer class="bg-gray-800 text-white py-6 mt-20">
        <div class="container mx-auto px-6 text-center">
            <p>&copy; {{ "now"|date("Y") }} EventPro. Tous droits réservés.</p>
        </div>
    </footer>
</body>
</html>
